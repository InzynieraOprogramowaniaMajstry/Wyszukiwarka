[tox]
envlist = py39
skipsdist = True
 
[testenv]
deps =
    pytest
    pytest-cov
commands = python -m pytest --cov=models --cov-report xml:coverage.xml --cov-config tox.ini --cov-branch
 
 
[coverage:run]
